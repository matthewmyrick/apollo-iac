name: Hello World

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: apollo-runners
    
    steps:
    - name: Say Hello
      run: |
        echo "🎉 Hello World from apollo-runners!"
        echo "Runner: $(hostname)"
        echo "Date: $(date)"
        echo "Testing connectivity..."
        curl -s https://api.github.com > /dev/null && echo "✅ GitHub API accessible" || echo "❌ Cannot reach GitHub API"
        echo "Sleeping for 5 seconds to see this running..."
        sleep 5
        echo "✅ Workflow completed successfully!"
